# 排盤引擎口徑（calendar-engine v1.0）

> 適用：`lib/solar_terms.js` ＋ `lib/calendar.js`，以及以它們為基礎的 `八字排盤.html`。
> 這份文件是**規則的權威來源**。程式註解、頁面說明、測試三者若有出入，以本文件為準，並視為 bug。
> 凍結日：2026-08-01。版本規則見文末。

---

## 1. 時間基準

- 一律視為**中國標準時間 UTC+8 的鐘錶時間**（牆鐘）。
- **不做真太陽時校正**，不做經度時差修正。
- **不隨裝置時區改變**：同一組輸入在任何時區的瀏覽器結果相同。實作上完全不碰本地時區——`epochMin()` 用 `Date.UTC` 把牆鐘當成無時區的數線刻度。
- 另有 `pillarsLocal(..., offsetMinutes)` 供研究層使用：日柱／時柱取當地牆鐘，年柱／月柱換算為 UTC+8 後再查節氣。**排盤器不使用這一層**，一律 UTC+8。

## 2. 節氣來源與精度

| 項目 | 內容 |
|---|---|
| 來源 | `lib/lunar.js`（lunar-javascript）的天文計算值，由 `tools/gen_solar_terms.mjs` 一次性 dump |
| 內容 | 只收**十二節**（月界），不收中氣 |
| 精度 | 到**秒**；儲存為 `YYYYMMDDHHMMSS` 的 UTC+8 牆鐘字串 |
| 範圍 | 1900–**2101**，共 2424 個節 |
| 查找 | 二分搜尋字串比較（`prevIdx`），零依賴、離線 |

為什麼多生一年到 2101：2100-12-07 大雪之後出生的順行者，起運要往後找一個節。表若止於 2100，`nextJie` 回 null，整組大運排不出來。2101 只作為「查得到下一個節」用，**不可用來排 2101 的盤**（由 `assertRealDate` 擋下）。

**同一份表**供年柱、月柱、起運、流月共用。不存在第二套節氣計算。

### 已知精度風險

天文台在其對照表附註中指出，數十年後的節氣時刻可能有數分鐘誤差。本表中距午夜 5 分鐘以內的節共 **11 個**，這些是「不同星曆模型可能算出差一日」的位置：

| 年 | 節 | 本表時刻 | 距午夜 |
|---|---|---|---|
| 1911 | 立夏 | 05-07 00:00:18 | 18 秒 |
| 1917 | 大雪 | 12-08 00:00:59 | 59 秒 |
| 2083 | 立春 | 02-03 23:58:22 | 98 秒 |
| 1948 | 驚蟄 | 03-05 23:57:53 | 127 秒 |
| 2014 | 驚蟄 | 03-06 00:02:16 | 136 秒 |
| 2084 | 芒種 | 06-05 00:02:44 | 164 秒 |
| 1982 | 小寒 | 01-06 00:02:35 | 155 秒 |
| 2016 | 小暑 | 07-07 00:03:21 | 201 秒 |
| 2085 | 小寒 | 01-04 23:56:19 | 221 秒 |
| 2055 | 芒種 | 06-05 23:55:59 | 241 秒 |
| 2093 | 立冬 | 11-06 23:55:46 | 254 秒 |

其中 **2083 立春**與 **2014 驚蟄**已與香港天文台《公曆與農曆日期對照表》核對，日期相符（見 §7）。其餘 9 個尚未取得第二來源。

## 3. 年柱

- 以**立春交接時刻**換年，精確到秒。**不以農曆正月初一換年。**
- 實作：由出生時刻往前找到最近的節，再依該節的序號回退至管轄立春，取該立春所在的公曆年推干支。
- 立春交接的那一秒即算新年（`stamp <= k` 的比較是閉區間）。

## 4. 月柱

- 以二十四節氣中的**「節」**換月，**不按農曆初一**。
- 月支對應：

| 月支 | 節 | 月支 | 節 |
|---|---|---|---|
| 寅 | 立春 | 申 | 立秋 |
| 卯 | 驚蟄 | 酉 | 白露 |
| 辰 | 清明 | 戌 | 寒露 |
| 巳 | 立夏 | 亥 | 立冬 |
| 午 | 芒種 | 子 | 大雪 |
| 未 | 小暑 | 丑 | 小寒 |

- 月干用**五虎遁**，由年干推：`月干 = ((年干%5)*2 + 2 + 節序) % 10`。

## 5. 日柱

- **JDN mod 60 純算術**，不查表。錨點：2000-01-01 為**戊午**（jiazi index 54，JDN 2451545）。
- **00:00 換日**。23:00 不換日（預設口徑，見 §6）。
- 日柱不受時區參數影響。

## 6. 子時（本工具兩派都支援）

23:00–23:59 稱**晚子**，00:00–00:59 稱**早子**。兩派的差別**只在日柱**：

| 輸入 | 算當日（預設） | 算次日 |
|---|---|---|
| 1990-06-14 22:59 | 庚戌 · 丁亥 | 同左 |
| 1990-06-14 23:30 | **庚戌** · 戊子 | **辛亥** · 戊子 |
| 1990-06-15 00:30 | 辛亥 · 戊子 | 辛亥 · 戊子 |

- **時柱兩派相同**：晚子一律以「翌日日干」起遁，所以 23:30 與次日 00:30 的時柱本來就一樣。
- **年柱、月柱兩派都不動**：它們由節氣的絕對時刻裁定，日柱怎麼算不會讓 23:30 這個時刻跑到別的節氣區間。
- 預設 `sameDay`（與 lunar-javascript 一致，407 例 golden 釘死）。使用者可在輸入頁切換；該開關**只在填入 23 時才出現**，並當場顯示兩派各自的日柱。
- 盤面在 `出生` 一行標註實際採用的口徑（`晚子·日柱算當日／次日`），使盤面自陳、不依賴使用者記憶。
- 偏好存於 `localStorage.paipan_late_zi`。這是流派偏好，不是出生資料。

## 7. 時柱

- 時支：23 時與 0 時皆為**子**；其餘 `floor((h+1)/2) % 12`。即 1–2 丑、3–4 寅、…、21–22 亥。
- 時干用**五鼠遁**：`時干 = ((日干%5)*2 + 時支序) % 10`。
- 起遁所用的日干：非 23 時＝當日日干；**23 時＝翌日日干**（兩派皆同）。

## 8. 起運

- 方向：**陽年男、陰年女順行；陰年男、陽年女逆行**。依年柱天干的陰陽與性別判定。
- 順行取**下一個節**，逆行取**上一個節**。用「節」，不用中氣。
- 換算：出生時刻到該節的分鐘差 → **3 日折 1 歲、1 日折 4 個月、1 時折 5 日**。
  - `總月數 w = floor(日數 × 4)`，`天數 e = round((日數×4 − w) × 30)`；`e ≥ 30` 時進位。
  - 年 = `floor(w/12)`，月 = `w % 12`。
- **分鐘參與換算**（不是只算整日）。秒不參與（輸入本身只到分）。
- 顯示為「N 年 N 個月 N 天後」，並同時列出距節氣的實際日數與時數，供核對。

## 9. 行年虛歲

- 公式：`虛歲 = 流年 − 命造年柱所屬干支年 + 1`。
- **起算年是年柱的干支年，不是西曆出生年**。一月一日到立春之間出生者，年柱屬前一個干支年（1990-01-20 的年柱是己巳＝1989），用西曆年會少一歲。
- 實作以 `yearGZ()` 反推：干支六十年一輪，`b.y-1` 與 `b.y` 兩個候選必中其一。
- 顯示只寫「41歲」；完整語義（`2030年庚戌，行年虛歲41歲`）放在 `title` 與 `.sr-only`。

## 10. 流月／流日

- 流月：自該流年**立春**起算十二個月，以十二節交接；月干由**流年年干**五虎遁推。跨公曆年仍按節令連續推算。
- 流日：該流月自本節當日起，至下一個節的前一日止（約 29–32 天）。日柱直接呼叫 `Cal.pillars(y,m,d,12,0,0)`，與命盤共用同一個 JDN 函式。

## 11. 支援範圍與防呆

| 界線 | 值 | 行為 |
|---|---|---|
| 引擎下界 | **1900 立春**（1900-02-04 08:44:03） | 之前拋錯「無管轄立春可回溯」 |
| 引擎上界 | 2100-12-31 23:59:59 | 2101 起拋錯「年份超出範圍」 |
| UI 開放範圍 | 1901-01-01 – 2100-12-31 | `<input type=date>` 與 `parseQuick` 雙重把關 |

引擎會拒絕：不存在的日期（2100-02-29、2026-02-30、2026-04-31）、不合法月份（13 月）、超出範圍的年份。`jdn()` 是純算術公式，不擋就會靜默溢出成一張看起來正常、其實不存在的盤。

## 12. 測試

| 檔案 | 內容 |
|---|---|
| `tools/test_calendar.mjs` | 對照 lunar-javascript 的 golden：469 例（隨機 240、早晚子 80、節氣邊界 ±1 秒 87、2100 年 42、日界 15、公曆邊界 8）＋反查對拍 6 組＋起運分鐘差 30 例 |
| `tools/test_calendar_boundary.mjs` | 不依賴外部庫的口徑回歸鎖：防呆、2100 起運、日界與子時兩派、時支、節氣表結構、天文台 24 點交叉核對 |
| `tools/test_calendar_tz.mjs` | 時區包裝層 |
| `tools/test_paipan.mjs` | 端到端：排盤／起運／大運／流年／流月／流日／反查／行年虛歲／晚子口徑；流月 × 引擎月柱 1901–2099 共 2388 個月 |

### 第二來源核對

計畫要求凍結前至少兩個獨立來源。沙箱的 npm 與 PyPI 皆被封鎖（403），無法取得 `sxtwl`、`astronomia` 等獨立實作，因此改用**香港天文台《公曆與農曆日期對照表》**（自有星曆，與 lunar-javascript 無血緣）作為第二來源。

- 已核對：2014、2083 兩年共 **24 個節的公曆日期，零不符**。
- 涵蓋天文台自己在附註中點名可能差一日的 **2083 立春**（本表 23:58:22，距午夜僅 98 秒）。
- **限制**：天文台只公布節氣**日期**、不公布時刻，所以這是日期層級的核對，無法驗證秒級精度。§2 表中其餘 9 個近午夜的節尚未取得第二來源。

## 13. 版本規則

```
v1.0  初次凍結（2026-08-01）
v1.1  修正不改口徑的小錯（例：防呆、效能、錯誤訊息）
v2.0  改變日界、節氣來源或起運口徑
```

- 版本號寫在 `lib/calendar.js` 的 `API.version`，頁尾由程式讀取顯示，**不在頁面寫死**。
- 凍結後，純 UI 修改不得改動 `lib/calendar.js` 與 `lib/solar_terms.js`。
- 任何會改變既有排盤結果的修改，必須提升版本號並重跑全部 golden。
