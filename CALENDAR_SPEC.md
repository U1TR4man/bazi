# 排盤引擎口徑（calendar-engine v2.1）

> 適用：`lib/solar_terms.js` ＋ `lib/calendar.js`，以及以它們為基礎的 `八字排盤.html`。
> 這份文件是**規則的權威來源**。程式註解、頁面說明、測試三者若有出入，以本文件為準，並視為 bug。
> 初次凍結：2026-08-01；v2.1 修訂：2026-08-18。版本規則見文末。

## 個人研究附層：月朔運 V0.2

主引擎口徑仍是本文件以下各節所述的節氣運。另有預設隱藏的 `lib/lunar_dayun.js` 個人研究層：
它共用原四柱、順逆、九步大運干支、立春流年與固定十年運長，只把起運距離的錨點改為前／後一個
「農曆初一所在的 UTC+8 民用日 00:00」。日期來自 `lib/lunar_new_moons.js`，由本地
`lib/lunar.js` 經 `tools/gen_lunar_new_moons.mjs` 生成；不是天文真合朔時刻。

V0.2 另以 `lib/dayun_compare.js` 比較雙起運與交運錯位，並記錄積朔 `A`、運內 `S`、相位角 `theta`。
這一層不改年柱／月柱、不覆寫 `ZipingDayun`，也不做天文真合朔、六候、權重、評分或吉凶判斷。
完整半開邊界、積朔精度、秘密入口、golden 與回滾方式見 `docs/月朔運V0.2狀態.md`。

---

## 1. 時間基準

- 一律視為**中國標準時間 UTC+8 的鐘錶時間**（牆鐘）。
- **不做真太陽時校正**，不做經度時差修正。
- **不隨裝置時區改變**：同一組輸入在任何時區的瀏覽器結果相同。實作上完全不碰本地時區——`epochMin()` 用 `Date.UTC` 把牆鐘當成無時區的數線刻度。
- 另有 `pillarsLocal(..., offsetMinutes)` 供研究層使用：日柱／時柱取當地牆鐘，年柱／月柱換算為 UTC+8 後再查節氣。**排盤器不使用這一層**，一律 UTC+8。

## 2. 節氣來源與精度

| 項目 | 內容 |
|---|---|
| 來源 | `lib/lunar.js`（lunar-javascript）的天文計算值，由 `tools/gen_solar_terms.mjs` 一次性 dump |
| 內容 | 只收**十二節**（月界），不收中氣 |
| 精度 | 到**秒**；儲存為 `YYYYMMDDHHMMSS` 的 UTC+8 牆鐘字串 |
| 範圍 | 1900–**2130**，共 2772 個節 |
| 查找 | 二分搜尋字串比較（`prevIdx`），零依賴、離線 |

為什麼表要蓋到 2130，而排盤上界只到 2100：

- 2100-12-07 大雪之後出生的順行者，起運要往後找一個節；表若止於 2100，`nextJie` 回 null，整組大運排不出來。
- 更遠的需求來自**大運跨九十年**。今天出生的嬰兒，第九步大運走到約 **2126**——這不是為了「2102 年出生的人」，
  是為了現在活著的人。表原本止於 2101 時，**2005 年以後出生者**的第九步只列得出 8 個流年。
- 2130 涵蓋 **2030 年以前出生者**（今日新生兒的第九步約走到 2126）。2031 年起部分出生時刻會超表
  （6000 組隨機時刻中 381 組），故支援上界保守訂在 2030。再往後不是不能延，是**那段資料沒有第二來源可核**
  （天文台只到 2100），為七十年後的假想使用者多背 26KB 未驗證資料不划算。
- **2031 年以後出生者**的部分時刻第九步會超出表：`yearsOfStep` 拋 `RangeError`，**不接受靜默截斷**。
- 2101 起的年份不可用來排盤（`assertRealDate` 擋下），只作為大運／流年／流月的查表範圍。

**2102–2130 的節氣值沒有第二來源可核。** 香港天文台的對照表只到 2100，那 29 年完全依賴
lunar-javascript 單一來源。下方的近午夜風險表因此只列到 2101——在一個整段都未經核對的範圍裡
挑幾個「近午夜」標成風險點，是假精確。

**同一份表**供年柱、月柱、起運、流月共用。不存在第二套節氣計算。

### 已知精度風險

天文台在其對照表附註中指出，數十年後的節氣時刻可能有數分鐘誤差。本表中距午夜 5 分鐘以內的節共 **11 個**，這些是「不同星曆模型可能算出差一日」的位置。
下表由 `tools/test_calendar_boundary.mjs` 逐格核對，時刻或排序與節氣表對不上就會測試失敗：

| 年 | 節 | 本表時刻 | 距午夜 |
|---|---|---|---|
| 1911 | 立夏 | 05-07 00:00:18 | 18 秒 |
| 1917 | 大雪 | 12-08 00:00:59 | 59 秒 |
| 2083 | 立春 | 02-03 23:58:22 | 98 秒 |
| 1948 | 驚蟄 | 03-05 23:57:53 | 127 秒 |
| 2014 | 驚蟄 | 03-06 00:02:16 | 136 秒 |
| 1982 | 小寒 | 01-06 00:02:35 | 155 秒 |
| 2084 | 芒種 | 06-05 00:02:44 | 164 秒 |
| 2016 | 小暑 | 07-07 00:03:21 | 201 秒 |
| 2085 | 小寒 | 01-04 23:56:19 | 221 秒 |
| 2055 | 芒種 | 06-05 23:55:59 | 241 秒 |
| 2093 | 立冬 | 11-06 23:55:46 | 254 秒 |

**11 個全部**已與香港天文台《公曆與農曆日期對照表》核對（見 §12）。結果：**10 個相符，1 個歧異**（1917 大雪）。

### 已知歧異：1917 大雪

| | 日期時刻 |
|---|---|
| 本表 | 1917-12-08 **00:00:59** |
| 香港天文台 | 1917-12-07 |

兩套星曆差不到一分鐘，但剛好跨過日界。**不手動改表**——`solar_terms.js` 是 generator 產物，手改一格會讓它與 lunar-javascript 的 golden 永久分岔，而且下次重生就沒了。`test_calendar_boundary.mjs` 反過來把這個歧異釘住：哪天表變了，測試會提醒要一併更新本文件。

影響面：只有 **1917-12-07 23:00 到 12-08 00:00:59** 出生的人，月柱在兩家之間會差一個月（亥月／子月）。

## 3. 年柱

- 以**立春交接時刻**換年，精確到秒。**不以農曆正月初一換年。**
- 實作：由出生時刻往前找到最近的節，再依該節的序號回退至管轄立春，取該立春所在的公曆年推干支。
- 立春交接的那一秒即算新年（`stamp <= k` 的比較是閉區間）。

## 4. 月柱

- 以二十四節氣中的**「節」**換月，**不按農曆初一**。
- 月支對應：

| 月支 | 節 | 月支 | 節 |
|---|---|---|---|
| 寅 | 立春 | 申 | 立秋 |
| 卯 | 驚蟄 | 酉 | 白露 |
| 辰 | 清明 | 戌 | 寒露 |
| 巳 | 立夏 | 亥 | 立冬 |
| 午 | 芒種 | 子 | 大雪 |
| 未 | 小暑 | 丑 | 小寒 |

- 月干用**五虎遁**，由年干推：`月干 = ((年干%5)*2 + 2 + 節序) % 10`。

## 5. 日柱

- **JDN mod 60 純算術**，不查表。錨點：2000-01-01 為**戊午**（jiazi index 54，JDN 2451545）。
- **00:00 換日**。23:00 不換日（預設口徑，見 §6）。
- 日柱不受時區參數影響。

## 6. 子時（本工具兩派都支援）

23:00–23:59 稱**晚子**，00:00–00:59 稱**早子**。兩派的差別**只在日柱**：

| 輸入 | 算當日（預設） | 算次日 |
|---|---|---|
| 1990-06-14 22:59 | 庚戌 · 丁亥 | 同左 |
| 1990-06-14 23:30 | **庚戌** · 戊子 | **辛亥** · 戊子 |
| 1990-06-15 00:30 | 辛亥 · 戊子 | 辛亥 · 戊子 |

- **時柱兩派相同**：晚子一律以「翌日日干」起遁，所以 23:30 與次日 00:30 的時柱本來就一樣。
- **年柱、月柱兩派都不動**：它們由節氣的絕對時刻裁定，日柱怎麼算不會讓 23:30 這個時刻跑到別的節氣區間。
- 預設 `sameDay`（與 lunar-javascript 一致，407 例 golden 釘死）。使用者可在輸入頁切換；該開關**只在填入 23 時才出現**，並當場顯示兩派各自的日柱。
- 盤面在 `出生` 一行標註實際採用的口徑（`晚子·日柱算當日／次日`），使盤面自陳、不依賴使用者記憶。
- 偏好存於 `localStorage.paipan_late_zi`。這是流派偏好，不是出生資料。

## 7. 時柱

- 時支：23 時與 0 時皆為**子**；其餘 `floor((h+1)/2) % 12`。即 1–2 丑、3–4 寅、…、21–22 亥。
- 時干用**五鼠遁**：`時干 = ((日干%5)*2 + 時支序) % 10`。
- 起遁所用的日干：非 23 時＝當日日干；**23 時＝翌日日干**（兩派皆同）。

## 7b. 四柱反查（v2.1 時間區間契約）

四柱只能指出「哪些時刻會得到這組四柱」，不能證明區間內哪一分鐘才是真實出生時間。
因此 `Cal.reverse()` 自 v2.1 起回傳**連續半開區間**，不再以某個整點代表整個時辰：

```text
{
  start: {y,m,d,h,mi,s},
  endExclusive: {y,m,d,h,mi,s},
  blockStartHour: 0｜23｜其餘奇數時,
  precision: "interval"
}
```

- 區間語義為 `[start, endExclusive)`。人看的 `11:00:00–12:59:59`，引擎實際保存為 `[11:00:00, 13:00:00)`。
- 節氣若在時辰中間交接，區間會**精確切在該秒**。例如 1901 小寒在 07:53:23，交節後四柱的窗口從 07:53:23 起，不可回傳 07:00。
- 頂層 `y/m/d/h/mi/s` 暫保留為 `start` 的相容別名；新程式必須讀 `start`／`endExclusive`，不可再把 `h` 當「時辰起點」。
- `lateZi:'nextDay'` 時，同一四柱可同時對應前一民用日 23:00–23:59 與次日 00:00–00:59；兩個窗口都保留，不合併、不漏掉前者。
- 排盤器選中反查結果後走 `castFromPillars()`：顯示原四柱、有效時間區間與大運順逆序列；**不呼叫 `castFromDate()`，不推起運歲／交運日，不顯示「精確至日」**。
- 知道確切出生時分時，應改用生日模式；只有那條路徑才可依 §8 計算起運。

## 8. 起運

- 方向：**陽年男、陰年女順行；陰年男、陽年女逆行**。依年柱天干的陰陽與性別判定。
- `Dayun.build()` 的性別只接受 `乾`／`坤`；空值或其他寫法會拋錯，**不得把未知性別靜默當成坤造**。
- 順行取**下一個節**，逆行取**上一個節**。用「節」，不用中氣。
- 換算：出生時刻到該節的分鐘差 → **3 日折 1 歲、1 日折 4 個月、1 時折 5 日**。
  - `總月數 w = floor(日數 × 4)`，`天數 e = round((日數×4 − w) × 30)`；`e ≥ 30` 時進位。
  - 年 = `floor(w/12)`，月 = `w % 12`。
- **分鐘參與換算**（不是只算整日）。秒不參與（輸入本身只到分）。
- 顯示為「N 年 N 個月 N 天後」，並同時列出距節氣的實際日數與時數，供核對。

## 9. 行年虛歲

- 公式：`虛歲 = 流年 − 命造年柱所屬干支年 + 1`。
- **起算年是年柱的干支年，不是西曆出生年**。一月一日到立春之間出生者，年柱屬前一個干支年（1990-01-20 的年柱是己巳＝1989），用西曆年會少一歲。
- 實作以 `yearGZ()` 反推：干支六十年一輪，`b.y-1` 與 `b.y` 兩個候選必中其一。
- 顯示只寫「41歲」；完整語義（`2030年庚戌，行年虛歲41歲`）放在 `title` 與 `.sr-only`。

## 9b. 「現在」的判定（今年／目前大運／本月／今日）

盤面上的四個「今」字標記，與四柱一樣走 **UTC+8 與立春／節氣口徑**，**不使用裝置本地時間**。

- 唯一來源是 `nowUtc8()`，回傳 UTC+8 牆鐘的年月日時分秒。全檔不得再各自取現在時間（`test_paipan.mjs` 靜態擋住）。
- **今年**＝現在時刻的**年柱所屬干支年**，由 `Cal.pillars(現在)` 反推，**不是公曆年**。
  每年 1 月 1 日到立春約 34 天，公曆年與干支年不同；用公曆年會讓「今」標在前一個流年的格子上。
- **本月**＝現在落在哪一個節到下一個節之間，比較用**節氣的完整時刻（到秒）**，不是只比日期。
  節可能在 23:58 交，只比日期會讓交節當天整天判錯。
- **今日**＝ UTC+8 的年月日。
- 節氣時刻與現在時刻都用 `Date.UTC(牆鐘)` 換成同一條無時區數線再比較，與 `calendar.js` 的 `epochMin()` 同一套語義。

### 大運精確交運（V2 現行）

`lib/dayun.js` 已由排盤器直接載入。精確交運日期、大運區間、流年分組、跨運標記與「今運」判定
全部使用同一模型，不得只替換其中一項。口徑：

| 項目 | 規則 |
|---|---|
| 交運日 | 出生日期 ＋ 起運的 年／月／日，順序**固定為 年 → 月 → 日**，每一步各自夾到月底 |
| 交運時刻 | 該日 **UTC+8 00:00**。不沿用出生時分——起運只算得到「日」，帶進時分是虛構精度 |
| 代價 | 交運日**整天**算新運。這是日期精度的必然結果，寫在這裡不藏 |
| 大運區間 | `[startAt, nextStartAt)` 半開；交運當刻即屬新運，不重疊、不留縫 |
| 十年加法 | 只加年並夾月底：`2020-02-29 + 10 年 → 2030-02-28`，不溢出成 03-01 |
| 流年區間 | 該年立春 → 次年立春 |
| 流年歸屬 | **以序號分組**：第一步交運後的第一個立春為起點，每步固定取十個。「每步十格、不重不漏」由構造保證 |
| 近立春例外 | 交運日落在二月初時，十年區間會含 9 或 11 個立春（實測 0.3%）。選擇保住十格，代價是那一格的立春可能早幾小時——而那一年正是跨運年、會被標「交」 |
| 跨運標記 | 大運交接落在流年區間內時標 `crossesDayun`；UI 畫「交」，並提供精確交運日期與下一步大運 |
| 支援範圍 | 出生年 ≤ **2030**（節氣表上界 2130）。超出時 `yearsOfStep` 拋 `RangeError`，**不接受靜默截斷** |

加法順序不是細節：`2020-02-29` 依序 +1年+1月 得 `2021-03-28`，合併成 +13 月得 `2021-03-29`，差一天。

完整九步、每步十個流年的支援範圍是 **1901–2030 出生**。2031 年後若後段大運超出已核節氣表，
排盤器仍顯示四柱與可計算的大運交接日期，但會明示不能完整列出九十個流年；不得靜默少格。

**V1／V2 差異（3000 張抽樣，`tools/report_dayun_v1_v2.mjs` 可重現）**

| | |
|---|---|
| 九步大運干支、順逆方向 | 100% 相同 |
| 四柱、流年干支、流月、流日 | V2 未碰這些函式 |
| 第一步起年改變 | 25.1%（−1 年 13.1%、不變 74.9%、+1 年 12.0%） |
| 流年格歸屬改變 | 9.0% |
| V1 起年 vs V2 交運時刻 | 平均差 90 天、最大 184 天 |

起年變動是移除整年四捨五入後的預期結果，不是回歸錯誤。

### 交運流年的盤面語義（2026-08-19）

大運按精確交運日換界，流年按立春換界，所以同一個流年可以先後落在兩步大運。盤面必須依下列規則顯示：

1. **起運流年要直接命名。** 大運題頭同時列「交運日」與該時刻所屬的立春流年；例如
   `1999.11.03 18:00` 乾造為 `2008.05.24 交運 · 起運流年 2008 戊子`，不可只從第一個完整流年
   `2009 己丑` 起列而讓 2008 消失。
2. **交運帶只在中途換運的流年出現。** 左段列立春至交運前一日，右段列交運日至下一個立春；
   交運日當天依 V2 的半開區間規則屬交後大運。交運前左段醒目、交運後右段醒目；
   左右固定等寬，寬度不代表作用時間或影響力。
3. **交運日前只顯示交前大運。** 首次起運之前沒有大運干支，交運帶寫「未交大運」，盤內不造一個空假柱。
4. **交運日後才顯示交界比較盤。** 後續換運年畫「交前／參照」與「交後／生效」兩柱，再接流年；
   即四柱原局＋兩柱大運＋流年，共七欄。交前欄降階，交後欄才是有效大運。
5. **研究未來年份時以所選時間判定。** 判定優先序為流日 → 流月區間 → 今天。點交運日前的流日只顯示舊運；
   點交運日當天或之後立即顯示「交前／參照」＋「交後／生效」。只選流月時按節氣的真實秒級起訖判斷；
   若交運日在該月內，兩柱平級標成「月內前段／月內後段」，不把整個月假定只屬一運。
6. **下一個流年恢復正常。** 只留交後大運＋流年，不把七欄比較狀態永久帶下去。

這一節只固定 UI 如何表達既有 V2 時間模型，沒有改 `calendar.js`／`dayun.js` 的計算或版本號。

### V1 回退邊界

V1 以 `出生年 + Math.round(起運歲)` 建整年範圍，無法表示一年中途換運；它已由 V2 取代，
但回退點保留在 Git tag `calendar-engine-v1.0`。若日後需要回退，必須連大運區間、流年分組、
矩陣歸屬與「今運」判定一起回退，不可混用 V1／V2。

## 10. 流月／流日

- 流月：自該流年**立春**起算十二個月，以十二節交接；月干由**流年年干**五虎遁推。跨公曆年仍按節令連續推算。
- 流日：該流月自本節當日起，至下一個節的前一日止（約 29–32 天）。日柱直接呼叫 `Cal.dayGZ(y,m,d)`，與命盤共用同一個 JDN 函式。

## 11. 支援範圍與防呆

| 界線 | 值 | 行為 |
|---|---|---|
| 引擎下界 | **1900 立春**（1900-02-04 08:44:03） | 之前拋錯「無管轄立春可回溯」 |
| 引擎上界 | 2100-12-31 23:59:59 | 2101 起拋錯「年份超出範圍」 |
| UI 開放範圍 | 1901-01-01 – 2100-12-31 | `<input type=date>` 與 `parseQuick` 雙重把關 |

引擎會拒絕：不存在的日期（2100-02-29、2026-02-30、2026-04-31）、不合法月份（13 月）、超出範圍的年份、非整數或超界的時分秒（小時 0–23、分秒 0–59）、未知的 `lateZi` 值、非數值／超界的 UTC offset。`jdn()` 與 `Date.UTC()` 都會正規化非法輸入；不先擋就會靜默算出一張看起來正常、其實不存在的盤。

`Cal.reverse()` 另拒絕非整數、倒置或超出 1900–2100 的年份窗。非法四柱回空陣列；日期／時間／選項格式錯誤則拋出明確錯誤。

## 12. 測試

| 檔案 | 內容 |
|---|---|
| `tools/test_calendar.mjs` | 對照 lunar-javascript 的 golden：469 例（隨機 240、早晚子 80、節氣邊界 ±1 秒 87、2100 年 39、日界 15、公曆邊界 8）＋反查對拍 6 組；每個反查窗口的首秒／末秒再正查；起運分鐘差 30 例 |
| `tools/test_calendar_boundary.mjs` | 不依賴外部庫的口徑回歸鎖：輸入防呆、2100 起運、日界與子時兩派、時支、反查連續區間、**1901–2100 全部十二節前／當／後一秒共 7200 點**、晚子次日雙窗口、節氣表結構、天文台 **132 點**交叉核對（含 1 個釘住的已知歧異）|
| `tools/test_calendar_tz.mjs` | 時區包裝層 |
| `tools/test_paipan.mjs` | 端到端：排盤／起運／大運／流年／流月／流日／反查／行年虛歲／晚子口徑／字型缺字／字體分工；另鎖 `1999.11.03 18:00` 的 2008 起運流年、2018 七欄交界、2019 回復單一大運；流月 × 引擎月柱 1901–2099 共 2388 個月 |
| `tools/test_chart_render.mjs` | 共用渲染器：欄數、分區、行年、輔柱，以及交前／交後 role 白名單與七欄輸出 |
| `tools/test_pwa.mjs` | 離線與安裝：路徑、快取策略、元素順序、`file://` 安全 |
| `tools/test_dayun_v2.mjs` | V2 大運引擎：加法順序與夾月底、區間半開無縫、交運日界三點、流年歸屬不重不漏、跨運標記、超表拋錯 |
| `tools/test_font.py` | 字型子集 cmap 覆蓋與 Sans/Serif 分工 |

### 第二來源核對

計畫要求凍結前至少兩個獨立來源。沙箱的 npm 與 PyPI 皆被封鎖（403），無法取得 `sxtwl`、`astronomia` 等獨立實作，因此改用**香港天文台《公曆與農曆日期對照表》**（自有星曆，與 lunar-javascript 無血緣）作為第二來源。

- 已核對 **11 個年份、132 個節的公曆日期**：1911、1917、1948、1982、2014、2016、2055、2083、2084、2085、2093。這 11 年涵蓋了本表**全部**距午夜 5 分鐘以內的節。
- 結果：**131 相符、1 歧異**（1917 大雪，見 §2）。
- 其中包含天文台自己在附註中點名可能差一日的 2083 立春（本表 23:58:22，距午夜僅 98 秒）——**相符**。
- **限制一**：天文台只公布節氣**日期**、不公布時刻，所以這是日期層級的核對，無法驗證秒級精度。
- **限制二**：香港在 1941 年前後及 1948 等年份曾實施夏令時間（1948 年為 5/2–10/31）。落在夏令時間區間內的節氣，日期比對不具參考性。本次要驗的節都不在該區間。

## 13. 版本規則

```
v1.0  初次凍結（2026-08-01）
v1.1  修正不改口徑的小錯（例：防呆、效能、錯誤訊息）
v2.0  精確交運日期、大運區間、跨運流年與今運判定整套接入（2026-08-17）
v2.1  反查改為秒級有效時間區間；補晚子次日雙窗口；反查不再虛構精確起運；收緊輸入防呆（2026-08-18）
```

- 版本號寫在 `lib/calendar.js` 的 `API.version`，頁尾由程式讀取顯示，**不在頁面寫死**。
- 凍結後，純 UI 修改不得改動 `lib/calendar.js`、`lib/dayun.js` 與 `lib/solar_terms.js`。
- 任何會改變既有排盤結果的修改，必須提升版本號並重跑全部 golden。
