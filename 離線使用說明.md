# 把八字排盤裝進手機，沒網路也能用

裝一次，以後開飛航模式也能排盤。
整個過程 30 秒，做完就不用再管它。

---

## iPhone

1. 用 **Safari** 打開 https://u1tr4man.github.io/bazi/
   （建議用 Safari，步驟最一致。其他瀏覽器也可能有，
   但如果分享選單裡找不到「加入主畫面」，就改用 Safari。）
2. 按螢幕**最下面正中間的分享鍵**（方框上面一個箭頭）
3. 手指往下滑，找到 **加入主畫面**，按下去
4. 保持「以 Web App 開啟」是開著的
5. 右上角按 **新增**

做完了。桌面會多一個紅色的「八字排盤」。

---

## Android

1. 用 **Chrome** 打開 https://u1tr4man.github.io/bazi/
2. 按右上角的 **⋮**（三個點）
3. 按 **安裝應用程式**（有些手機寫「加到主畫面」，一樣的）
4. 按 **安裝**

做完了。

---

## 怎麼知道成功了

第一次打開的時候，畫面**最下面**會冒出一條字：

> 已準備完成，可離線使用

看到這句就是成了。它幾秒後會自己不見，不用管。

---

## 不放心的話，測一下

1. 打開**飛航模式**
2. 點桌面那個紅色圖示
3. 能排盤 = 成功

---

## 三種可能遇到的狀況

**沒看到「已準備完成」那句**
就是網路比較慢而已。在網頁上停 10 秒，或重新整理一次。

**下面冒出「排盤已有新版」**
按 **重新載入** 就好。
不按也能繼續用，只是用的是舊版本。

**怎麼弄都還是舊的**
長按桌面圖示 → 刪掉 → 用 Safari 重新打開網站 → 重新加入主畫面。
不會弄丟任何東西，這個工具本來就不會存你的生日。

---

## 懶人備案

覺得上面太麻煩的話：

把 **八字排盤.html** 這個檔案直接存到手機或電腦裡，
用瀏覽器打開，一樣能排盤，而且**永遠**不需要網路。

一個檔案就是全部，沒有其他東西要裝。
